/**
 * Skeleton state for the Work page's project grid — Implementation
 * Blueprint Section 1.4. Rarely actually triggered in production given
 * this site's fully static rendering, but cheap to have in place.
 */
export default function WorkLoading() {
  return null;
}
